<h2>Edit recipe image</h2>

<%= render "form.html", changeset: @changeset,
                        action: recipe_image_path(@conn, :update, @recipe_image) %>

<%= link "Back", to: recipe_image_path(@conn, :index) %>
